# Exercise checks

Exercise checks will be added with the guided exercise track. The first build
focuses on complete incident workflows under `drills/`.

Checks execute inside the lab host so Bash and PowerShell users receive the
same result.
